To run the app use the following commands

mvn package
java -jar target/bots_crew_test_task-1.0-SNAPSHOT

Or use the .jar file i have included inside my letter or one in the root of the project
java -jar BookShelf.jar
